namespace CTEditor.SharedKernel.Events
{
    /// <summary>
    /// Marca un tipo como evento de dominio: un hecho que YA ocurrió (nombre en pasado)
    /// e inmutable. Pasó, y punto. Ver A.6.
    /// </summary>
    public interface IDomainEvent
    {
    }
}
